image: python:latest

stages:
  - Test
  - Release

pypi:
    stage: Release
    script:
        - pip install -U twine
        - python setup.py sdist
        - twine upload dist/*
    only:
        - tags
